describe('Main', () => {
    describe('Method A', () => {
        context('Case 1', () => {
            it('should happen blabla', () => {
                // espera que aconteça
                // Entra de dados / método sum(1, 2)
                // Espera retornar (4) => true | false => broken test
            })
        });
        context('Case 2', () => {

        });
    });
    describe('Method A', () => {

    });
});